image: golang:latest

before_script:
  - apt-get update -qq && apt-get install -y -qq nmap
  - go get

stages:
  - test
  - build

test:
  stage: test
  script:
    - make generate
    - make test

build:
  stage: build
  script:
    - make generate
